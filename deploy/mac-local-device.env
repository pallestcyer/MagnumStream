# Production Local Device Configuration for Mac
# Copy this to .env on the Mac device

# Local device mode - no Supabase needed
USE_SUPABASE=false
NODE_ENV=production
PORT=5000

# Google OAuth for Drive integration (get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# Session secret for local auth
SESSION_SECRET=change-this-to-random-secret-in-production

# Optional: Custom projects directory
# PROJECTS_DIR=./projects

# Optional: Custom ngrok subdomain (requires paid ngrok plan)
# NGROK_SUBDOMAIN=magnumstream-device-1